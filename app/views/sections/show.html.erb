<h1><%=link_to @chapter.title, chapter_path(@chapter) %>/<%=@section.heading %></h1>
<ul>
  <%@section.lessons.each do |lesson|%>
  <li><%=link_to lesson.name, section_lesson_path(@section,lesson)%></li>
  <% end %>
</ul>
<p><%= link_to "New Lesson", new_section_lesson_path(@section) %></p>
